# AI 训练师个人网站构建提示词

你是一个顶级的全栈开发工程师和 Web 视觉交互专家。请根据当前项目目录下的 `DESIGN.md` 设计规范和 `.cursor/rules/project-context.mdc` 项目上下文，为我构建一个极具高级感和沉浸交互的「AI 训练师个人网站」。

## 1. 核心技术栈要求
- **框架**: 使用 Next.js (App Router) 或 React 构建单页/多页应用。
- **样式**: 使用 Tailwind CSS 进行样式管理，必须严格遵循 `DESIGN.md` 中定义的 CSS 变量（色彩、间距等），不要使用默认的高饱和度色板。
- **动效**: 使用 Framer Motion 或 GSAP 实现页面滚动（Scroll-story）和文本显现（Text Reveal）动效。
- **3D交互**: 使用 Three.js / React Three Fiber 在首屏（序章）实现一个基于鼠标跟随交互的「3D 粒子黑洞」或「深空漩涡」效果（对标 V0 模板的 3D 质感和流畅度）。

## 2. 页面模块与结构
请构建一个平滑滚动的极简网页，依次包含以下四个核心 Section：
1. **序章 (Prologue)**: 全屏 Hero 区。大标题（使用衬线体）+ 3D 交互背景。标题文字需采用错落有致的高级排版，并伴随顺滑的遮罩入场动画 (Mask Reveal)。
2. **数字痕迹 (Digital Traces)**: 作品集展示。使用纯黑底色+细线框的极简卡片（Bento Grid 或 List 布局），Hover 时卡片边框需有轻微泛光（Glow）和反差色效果。
3. **关于我 (About Me)**: 极简文字排版区。大量留白，突出文字的阅读体验。中文必须使用思源宋体（大标题）与思源黑体（正文）混排，传达科技与人文交织的理念。
4. **边角碎念 (Marginal Musings)**: 随笔流。类似极简日志的时间线布局，文字需随着页面的向下滚动自然浮现（ScrollReveal）。

## 3. 动效与质量红线
- 必须实现 **L3 级别的沉浸体验**：
  - 至少 1 个 WebGL/3D 签名交互时刻（位于首屏）。
  - 至少 1 处视差滚动（Parallax）或元素吸附（Pin）效果。
  - 每一个新模块滚动进入视窗时，内部元素必须有优雅的交错入场动画（Staggered Fade-Up）。
- **移动端适配**: 确保 3D 效果在移动端性能良好（控制粒子数量），或优雅降级为静态深空线稿背景。
- **代码结构**: 必须高度模块化（分为 `components`, `styles`, `utils` 等），CSS 变量必须提取到全局作用域。

请一步步分析并开始编写代码，先搭建基础的路由与骨架，再应用 Tailwind 样式，最后逐步注入 3D 交互和滚动动效。务必保持代码的高内聚低耦合。